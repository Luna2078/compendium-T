{
  "name": "@ct/persistencia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bloco 2 — camada de dados: migrations (Supabase/Postgres) + loader que mapeia linhas para os objetos do motor (Personagem + EstadoDeSessao). O motor permanece intocado.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@ct/compendio": "*",
    "@supabase/supabase-js": "^2.109.0",
    "pg": "^8.22.0"
  }
}
