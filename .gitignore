# ── SEGREDOS E VARIÁVEIS DE AMBIENTE — NUNCA versionar (nem em repo privado) ──
# Prevenção para o Bloco 2 (chaves de Supabase). Vale para TODO o monorepo (raiz + apps + packages).
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json
.vercel

# dependências
node_modules/
# build do Next — qualquer app/pacote do monorepo (o antigo site/ virou apps/*)
**/.next/
**/out/
*.tsbuildinfo
site/.next/
site/out/
# cache de extração (páginas renderizadas / imagens)
extracao/cache/
# binários do poppler (baixados localmente — não versionar)
extracao/poppler-bin/
data/**/imagens/
# PDFs dos livros (NUNCA versionar — tamanho + direitos autorais)
pdfs/
*.pdf
# config local do Claude Code (permissões pessoais)
.claude/settings.local.json
# companion de brainstorm
.superpowers/
# SO
Thumbs.db
.DS_Store

# Relatórios gerados pelos testes (ficha, ataque, auditoria de cobertura, varreduras).
# São artefatos de diagnóstico regenerados a cada `vitest run`, não fonte.
# Sem barra inicial: os testes os escrevem relativos à raiz dos DADOS, que mudou de lugar
# na migração para monorepo — o padrão precisa valer em qualquer diretório.
ataque-thaide.txt
audit-efeitos.txt
cobertura-motor.txt
conjuracao-nyra.txt
denso-vharo20.txt
ficha-thaide.txt
item-condicional.txt
lefou-tormenta.txt
multiclasse-korran.txt
varredura-exprs.txt
casos-oraculo.txt
dev.log
